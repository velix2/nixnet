#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

for flake in "$SCRIPT_DIR"/../examples/*/flake.lock; do
    dir="$(dirname "$flake")"
    echo "Updating nixnet in $(basename "$dir")..."
    nix flake update nixnet --flake "$dir"
done

echo "Done."
